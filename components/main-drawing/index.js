import React, {Component} from 'react';
import joint from 'jointjs';

require('./main-drawing.less');

export default class MainDrawing extends Component {
  render() {
    console.log(joint);
    return (
      <div className="main-drawing__field">
        main-drawing__field11
      </div>
    );
  }
}
